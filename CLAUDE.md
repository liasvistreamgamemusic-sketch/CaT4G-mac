# CaT4G - Claude Code 設定

# 機能実装はサブエージェントを使用すること

## プロジェクト概要

- **名前**: CaT4G (Chords and Tabs for Guitar)
- **種類**: ギターコード譜管理デスクトップアプリ
- **技術**: Tauri 2.0 + React 18 + TypeScript + Rust + SQLite
- **進捗**: フェーズ1完了 → フェーズ2（スクレイピング）準備中

---

## ディレクトリ構造

```
cat4g/
├── src/                    # React フロントエンド
│   ├── components/         # UIコンポーネント
│   ├── lib/               # ユーティリティ、DB API
│   ├── hooks/             # カスタムフック
│   └── types/             # TypeScript型定義
├── src-tauri/             # Rust バックエンド
│   ├── src/               # Rustソース
│   │   ├── lib.rs         # Tauriエントリ
│   │   ├── scraper.rs     # スクレイピング
│   │   └── parsers/       # サイト別パーサー（TODO）
│   └── migrations/        # SQLマイグレーション
├── specs/                 # 仕様書（参照のみ）
├── docker/                # Docker設定
└── Plans.md               # 実装計画
```

---

## コーディング規約

### TypeScript/React

- 関数コンポーネント + hooks
- Tailwind CSS（カスタムダークテーマ）
- 厳密な型定義（`strict: true`）
- パスエイリアス: `@/` → `src/`

### Rust

- Edition 2021
- `thiserror` でカスタムエラー
- `scraper` クレートでHTML解析

### 命名規則

| 種類           | 規則       | 例                  |
| -------------- | ---------- | ------------------- |
| コンポーネント | PascalCase | `AddSongModal`      |
| 関数/変数      | camelCase  | `fetchChordSheet`   |
| 型             | PascalCase | `SongWithDetails`   |
| Rust関数       | snake_case | `fetch_chord_sheet` |
| DBカラム       | snake_case | `artist_id`         |

---

## 開発コマンド

```bash
# Docker 開発
docker compose up frontend    # フロントエンドのみ
docker compose up dev         # 全環境（Rust含む）

# ローカル開発
npm run dev                   # Vite
npm run tauri dev             # Tauriアプリ

# ビルド・チェック
npm run build
npm run lint
npm run format
```

---

## データベース（10テーブル）

- `artists` - アーティスト
- `songs` - 曲（メタデータ）
- `sections` - セクション（Intro, Verse等）
- `lines` - 行（歌詞+コード位置JSON）
- `tags`, `song_tags` - タグ管理
- `playlists`, `playlist_songs` - プレイリスト
- `chord_preferences` - カスタム押さえ方
- `settings` - アプリ設定

---

## 仕様書参照

| 機能             | 仕様書                                                 |
| ---------------- | ------------------------------------------------------ |
| スクレイピング   | [04_backend_scraping.md](specs/04_backend_scraping.md) |
| オートスクロール | [05_autoscroll.md](specs/05_autoscroll.md)             |
| メトロノーム     | [06_metronome.md](specs/06_metronome.md)               |
| 転調・コード     | [07_transpose.md](specs/07_transpose.md)               |
| プレイリスト     | [08_playlist.md](specs/08_playlist.md)                 |
| UI デザイン      | [09_ui_design.md](specs/09_ui_design.md)               |

---

## テーマカラー

```
背景: #0a0a0f (primary), #1a1a25 (surface)
アクセント: #a855f7 (purple)
テキスト: #f8fafc (primary), #94a3b8 (secondary)
```

---

## 編集ルール

**編集可**: `src/`, `src-tauri/src/`, `Plans.md`
**参照のみ**: `specs/`（仕様変更が必要な場合は相談）

<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Jan 8, 2026

| ID   | Time    | T   | Title                                                          | Read |
| ---- | ------- | --- | -------------------------------------------------------------- | ---- |
| #293 | 5:17 PM | 🔵  | Second Screenshot Analysis - Additional Chord Display Evidence | ~292 |
| #292 | "       | 🔵  | First Screenshot Analysis - Chord Fingering Display Issue      | ~249 |

</claude-mem-context>
